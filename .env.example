# Copy to .env and fill in — or just paste the key in the app sidebar (bring-your-own-key).
OPENAI_API_KEY=
# Optional: any OpenAI-compatible endpoint (Groq, OpenRouter, Azure gateway, local vLLM…)
OPENAI_BASE_URL=
# Optional: default model
OPENAI_MODEL=gpt-4o-mini
